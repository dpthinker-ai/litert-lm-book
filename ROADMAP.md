# 书稿修订 Roadmap

日期：2026-09-05

基线：[v0.1.0](https://github.com/dpthinker-ai/litert-lm-book/releases/tag/v0.1.0)，提交 `f6f6a5727dc1d4516705b489e583bbd0757233fc`。

范围：技术正确性、文字自然度、内容与实测完备性。

进度（2026-09-05）：M1 的 C01–C08 已完成；M2 已关闭，未采用样稿且原文已恢复；M3 的 E01–E03 已完成；M4–M5 尚未开始。

## 目标与范围

保留现有 11 章、尾声和附录的主体结构，修正已确认的技术问题，减少重复导读与机械代码解释，并补上与端侧部署核心问题直接相关的测量。完成后，读者应能区分实现事实、理论推导、实际观测和未验证部分，并据此判断设备配置与优化方案。

本轮继续锁定 LiteRT-LM v0.13.1。书稿版本号与上游运行时版本号分别管理。`v0.1.0` 保持不变，后续修订通过新提交保存。

参考 Modular Handbook 的目标负载评估、用户时延和资源观测方法；本轮不扩展为服务端推理综述。连续批处理、分布式并行、推理路由、跨区域部署等不新增独立章节。NPU 成功部署、完整 kernel 优化教程与运行时版本迁移也不作为本轮完成条件。

## 阶段总览

| 阶段 | 优先级 | 状态 | 主要成果 | 完成条件 |
|---|---|---|---|---|
| M0 基线保存 | — | 已完成 | v0.1.0 Tag 与 Release | 远端 Tag 指向上述提交 |
| M1 技术纠错 | 最高 | 已完成（8/8） | 8 项已确认问题修正，相关图表与附录同步 | 逐项核对证据，无已知跨章矛盾，构建检查通过 |
| M2 文字精简 | 高 | 已关闭（样稿未采用，原文已恢复） | 小范围样稿及逐章精简 | 样稿风格经作者确认；改写节完成独立审校 |
| M3 核心实测 | 高 | 已完成（E01–E03） | 同一目标手机上的内存、持续性能与用户时延案例 | 条件完整、原始记录可复算，正文结论不超出测量范围 |
| M4 内容补充 | 中 | 未开始 | 量化对照与多模态端到端案例 | 可比条件成立并完成测量；不具备条件的项目明确保留为开放项 |
| M5 全书复核 | 发布前必做 | 未开始 | 修订书稿、HTML 与 PDF | 技术一致性、文字审校、全套检查与 PDF 阅读检查通过 |

M2 样稿未采用，批量精简已关闭。当前顺序为 M1 → M3 → M5，后续文字修正保留解释与证据的完整性。M3、M4 的设备与产物准备可穿插进行，实验结果纳入正文后，相关章节须再次审校。M4 暂时无法完成时，不阻塞已完成纠错成果的保存，但不能宣称实测补充已全部完成。

## M1：先修正确性与跨章一致性

**状态：已完成（2026-09-05），C01–C08 全部通过本阶段验收。**

每项按“证据核对 → 局部修改 → 搜索关联表述 → 检查”处理。原则上按独立问题提交；同一问题在正文、术语表、图注和习题答案中的修改放在同一提交。

| 编号 | 状态与提交 | 问题与修改方向 | 主要落点 | 验收依据 |
|---|---|---|---|---|
| C01 | 已完成；88282a5、d240473 | signature 定义不再把固定形状作为一般属性；分别说明具名入口、静态维度与动态维度 | 第 1 章 1.1、附录 A；交叉核对第 4、8 章 | 冻结源码的动态模型识别与执行路径；静态基准实例不能替代一般定义 |
| C02 | 已完成；71e39bc | 区分 Session 初次 Clone 的共享、后续上下文分离/保存，以及多候选 KV 变换 | 第 8 章表 8-1、相关正文、图 8-4；对照第 3、6 章和附录 E | `CloneContextHandler`、上下文分离与执行器复制路径；注明后端条件，不把后续复制归为初次 Clone 固定成本 |
| C03 | 已完成；f7f1078 | 分开说明权重流量下降的条件与吞吐受益的条件 | 第 7 章 7.1 及相关表格 | 位宽、传输表示与瓶颈的因果关系清楚；不以 bandwidth-bound 作为字节数下降的必要条件 |
| C04 | 已完成；d240473 | 统一 Mac MTP 记录范围：主矩阵无强制开启组，另有自然代码单次摘要记录 | 第 9 章、附录 C/D、相关实验说明 | 对照 `mtp.csv` 与 `mtp_ceiling.md`；列清原始证据缺失项，不能用摘要记录推断稳定加速比 |
| C05 | 已完成；357d958 | 修正音频分块取整相等条件：边界对齐是充分条件，不是必要条件 | 第 10 章 10.3.2 | 复算 C=4、R=3、S=7 的反例，并核对逐块有效长度累加实现 |
| C06 | 已完成；212e926 | 修正 TinyLlama 时间与来源体例；缩小前言“端侧能力相当于旧云侧模型”的推断 | 前言及脚注 | 区分训练启动与特定模型版本发布；缺少同任务质量对照时，仅陈述已获来源支持的功能与部署事实 |
| C07 | 已完成；88282a5 | 不再将 prefill/decode 简化为分别使用 Run/RunAsync；补清同步与异步条件 | 第 1 章 1.1；对照第 4、8 章 | 冻结版实际调用分支；保留 CPU/GPU、NPU 与其他后端路径的区别 |
| C08 | 已完成；f7f1078 | 区分持久模型文件、Engine 所持资源与进程的生命周期 | 第 7 章开头；对照第 11 章与尾声 | Engine 销毁与重建契约；不再把三种生命周期等同 |

交叉核对同时覆盖部分导言、SVG 图中文字与 aria-label、术语表、练习答案。除已确认问题外，新发现的疑点须先验证再修改；不以“更严谨”为理由批量增加限定句。

**M1 完成标准**：8 项问题均有明确处理结果；涉及代码的断言与冻结源码相符；相关章节之间口径一致；文风机检、来源引用、源码锚点、目录与 HTML 链接检查通过。此时可形成 `v0.1.1` 候选，但是否发布另行决定。

### M1 验收结果

- 以 LiteRT-LM v0.13.1（`a0afb5a56acd`）源码核对实现断言；同步正文、术语表、实验说明、习题答案与图 8-4。音频取整反例已独立复算。
- 独立审校覆盖受影响章节及改写节的第二段、末段，未发现阻塞问题；审校提出的局部断句与跨章表述问题已修正。
- 全书构建通过：22 个内容单元一致性检查、60 个脚注定义与 75 次引用、150 个唯一源码锚点、27 个 HTML 页面中的 1,408 处本地引用检查通过；文风检查零命中，构建无警告。
- 图 8-4 已渲染并目视检查，文字完整且无重叠；差异空白检查通过。本阶段未新增测量或生成 PDF，相关验收分别保留在 M3/M4 与 M5。
- 作者新增的“有字面表达时直接陈述”要求已原文写入 [.agents 的 book-review 技能](.agents/skills/book-review/SKILL.md)，并同步 [.claude 入口](.claude/skills/book-review/SKILL.md)（提交 `b9976d9`）。新增具体审校项，两处技能格式检查通过；本轮改稿已按此复核，后续修订继续适用。

## M2：以阅读效果为准，定点减少模板感

**状态：已关闭（2026-09-05），不计为精简验收完成。作者反馈样稿过于精简、阅读效果不如原文；两处正文已恢复至 M1 完成版，停止批量推广。**

后续修改保留原文的解释层次、铺垫与代码对应关系，只处理能够明确指出的重复、不通顺和刻意修饰。不以压缩篇幅为目标，不因出现编号或小结就删除其内容。

以下样稿方案作为未采用的历史计划保留，不再执行：原计划准备两处短样稿，包括第 2 章小结，以及第 11 章一段含连续代码编号解释的内容。每处保留原文与改稿对照，说明删去的重复信息及保留的机制依据。作者确认风格后，再逐章推广；不先做整书同义替换。

### 执行状态

| 工作 | 状态 | 结果与位置 |
|---|---|---|
| S01 第 2 章小结样稿 | 未通过，已恢复 | 撤回 `97bd367` 的正文修改，恢复原有小结 |
| S02 第 11 章代码解释样稿 | 未通过，已恢复 | 撤回 `a396132` 的正文修改，恢复原有代码解释 |
| 两处原文与改稿对照 | 保留为未采用样稿 | [M2 样稿对照](M2_SAMPLES.md) 已标明未获认可，不作为后续风格范例 |
| 样稿风格确认 | 未通过 | 作者认为精简过度；后续以保留解释完整性、局部修正为准 |
| 逐章精简及相关部分导言 | 未开始 | 按下表的范围逐章修改、核对关联表述并独立审校 |

样稿技术检查记录（不代表风格验收）：两处改写节的第二段与末段完成独立审校，未发现阻塞项。涉及实现的表述已核对 v0.13.1 源码；代码块、脚注定义、章节标题均保持原样。全书构建通过，文风检查零命中，60 个脚注定义、75 次引用、150 个唯一源码锚点与 1,408 处 HTML 本地引用检查通过。正文预览已恢复原文；并排对照页标明样稿未采用。

### 逐章推广范围

| 修改类型 | 优先范围 | 具体处理 |
|---|---|---|
| 重复导读与小结 | 四个部分导言、各章开头与结尾 | 部分导言交代共同问题；章首进入具体问题；保留有助于理解和回顾的总结，仅删除无新增作用的重复 |
| 机械代码解释 | 第 3、9、11 章优先，再检查第 4、5、8、10 章 | 保留逐步解释及其与代码编号的对应；在具体段落中补清状态、所有权、成本或失败条件，仅删确实多余的复述 |
| 审查报告口吻 | 第 7 章 7.7，尤其 7.7.9；各章密集限定段 | 正文保留与模型、缓存、后端和设备预算有关的判断；通用发布记录模板可移至附录 C，避免两处重复 |
| 连续抽象表述 | 全书逐章检查 | 用具体对象与因果关系替代空泛过渡；保留真正影响结论的版本、模型和测量条件 |

按章完成修改与检查后再进入下一章。使用 `book-review` 流程审校，每次一章。新增或改写节的第二段与末段依 `CLAUDE.md` 第五节第 14 条交由独立审校会话检查，不能由撰写该章的同一会话自评代替。

**M2 完成标准**：关键内容和证据完整保留；没有重复介绍同一章节路线；代码解释能够回答设计与成本问题；必要限制仍紧邻相关结论。机检通过是基础，作者的实际阅读判断决定样稿是否推广。不使用“AI 检测分数”或删字比例作为验收指标。

## M3：补齐端侧核心问题的实测

**状态：已完成（2026-09-05）。HONOR MEP-AN00 / Gemma 4 E4B / GPU OpenCL 同机案例，采用 4 组共 57 个成功请求；持续组完成 44 请求、901.7 s。**

优先选择同一台可用手机与同一模型产物，完成以下三个相互关联的实验。实验前固定运行时/SDK、模型 SHA-256、设备型号、系统与驱动、后端、线程与采样设置、输入和输出长度、缓存及供电条件。先验证完整生成与正常停止，再测量性能。

| 编号 | 要回答的问题 | 最小实验与输出 | 回填位置 |
|---|---|---|---|
| E01 | 文件体积与运行内存峰值差多少？ | 分别记录加载、prefill、decode、释放阶段的可用内存指标，注明计量工具、采样频率与统计范围；不能把进程 RSS 直接当作全部 GPU/NPU 内存 | 第 1、6、7 章与附录 D |
| E02 | 起始吞吐能持续多久，后续为何变化？ | 对固定负载持续运行，记录吞吐、温度与可取得的功率或能量数据；预先写明时长及停止条件，至少覆盖升温阶段；若未稳定，明确报告 | 第 1、8 章与附录 D |
| E03 | 用户等待多久，输出是否平稳？ | 从应用调用开始记录输入处理、prefill、首次可见文本、后续回调和结束时间；区分回调文本片段与 token，分别报告冷/热状态 | 第 2、5 章与附录 D |

E03 若能取得逐 token 时间，再报告 ITL；否则只报告可见文本片段间隔，不能用回调间隔冒充 ITL。先做小规模试运行检查采集方法，再确定重复次数；尾延迟分位数必须有足够样本，不能由三次运行估计 P99。

新实验记录单独保存在 `experiments/data/` 的日期目录，保存完整命令或程序版本、输入、原始日志、元信息及汇总方式，不覆盖 v0.1.0 原始数据。功率不可测时，不用温度或电池百分比代替功率；保留该指标缺口。设备或模型不同的结果分别列示。

**M3 完成标准**：至少有一个目标手机案例能够同时解释内存预算、持续吞吐和用户时延；图表可从归档记录重算。测不到的分项就地注明，不对同步、访存或热降频作缺乏证据的单因归因。

### M3 验收结果

| 编号 | 状态 | 已交付内容与证据边界 |
|---|---|---|
| E01 | 已完成进程内存测量 | 加载、prefill、decode、释放边界的 RSS/PSS/Swap 快照，500 ms 采样与生命周期 VmHWM；不作为完整 GPU 分配或部署总内存 |
| E02 | 已完成持续测量 | 901.7 s、44 请求全部正常结束；逐轮吞吐、当前 HAL 温度、系统热状态与可重算 SVG；结束仍升温，未宣称热稳态，功率缺口保留 |
| E03 | 已完成客户端测量 | 设备单调时钟记录输入准备、prefill、首段文本、全部回调与结束；区分新进程首次请求和已加载引擎后续请求；前后各 5 请求无内存采集对照，未冒充屏幕时延或 ITL |

原始目录索引为 [M3_RUNS.json](experiments/data/2026-09-05/M3_RUNS.json)，汇总与图表由 `m3_report.py` 重算。各次原始输入/输出、执行参数、构建来源、模型与库哈希均已归档；采样器配置错误和 CPU 采样回退的试运行另列，不混入正式 GPU 统计。现场条件记录了带壳、无主动散热、未开性能模式、USB 供电，以及作者估计的 22–25 °C 室温。

正文更新第 1、2、5、6、7、8 章，附录 C/D 补齐方法、数据、计量范围与复现命令，附录 A 补充 RSS/PSS。原文的推导与代码解释保留；新增段落经过逐章独立审校。源码、文风、引用、目录与 HTML 构建检查通过，图 D-1 已检查显示。PDF 全套检查保留在 M5，本阶段没有发布新版 PDF 或 Release。

M3 的完成不覆盖完整 GPU 分配、可靠功率、CPU/GPU 频率分解或 Android 屏幕显示事件。这些缺口在相关结果旁明确列出；当前案例只能用于本设备、文件、输入和执行配置。

## M4：补充质量与多模态案例

| 编号 | 工作 | 前置依赖 | 验收条件 |
|---|---|---|---|
| E04 量化对照 | 同一基础 checkpoint 的至少两档可比量化产物；固定任务集，比较任务质量、文件大小、内存、TTFT 和吞吐 | 可获取或可导出的模型产物、冻结运行时支持、可说明的量化与校准设置 | 同时报告质量与性能；产物不兼容或其他条件变化时，缩小归因范围；仅有位宽不同的文件不能视为严格对照 |
| E05 多模态案例 | 先完成一个图片输入案例，保存输入尺寸、预处理配置、有效 visual token、时延、内存和实际输出 | 可运行的视觉模型产物与输入样本 | 说明 token 数是直接观测还是模型结构复算；完成输入到输出的整条路径，并记录一个可复现的输入错误案例 |

音频端到端案例可在视觉案例完成后补充。NPU 成功部署、双 tokenizer、Python/C++ 一致性、分段并行加载和缓存开关对照继续作为后续项，除非实验发现它们直接影响本轮结论。

若 E04/E05 条件不具备，保留现有“未做”或“未完成”标注，并准确描述限制。本轮可先交付 M1–M3 成果，不能把实验方案本身写成实测证据。

## M5：全书复核与候选版本

1. 回看 M1 的每项问题，检查 M2 精简和新实验解释是否再次改变技术含义；复核正文、图表、附录与练习答案的一致性。
2. 独立复算新增表格与公式，检查统计量、单位、模型身份、版本和实验条件；来源引用遵守正文与附录不重复收录的体例。
3. 使用冻结源码执行书稿构建。源码锚点检查通过只证明位置有效，仍须人工确认引用支持相邻断言。
4. 按 `CLAUDE.md` 要求执行 `scripts/check_*.py` 全套检查，按各脚本参数和前置产物顺序运行；生成 PDF 后检查目录、书签、脚注、数学公式和全部受影响图表，抽查未改章节的版式回归。
5. 核对 `BOOK_PLAN.md` 的开放项和前言中的读者承诺与实际成果相符；只更新当前状态，不恢复逐章审校流水账。

**候选版本的完成条件**：已确认的技术错误全部修复；修改节独立审校通过；新增实测可复算且有明确适用范围；HTML/PDF 检查通过；剩余开放项清楚列明。M1–M3 完成并通过全书复核后，可准备 `v0.2.0` 候选。M4 的完成状态须写入候选版本说明。

M1、M3 已完成；M2 已关闭，正文已恢复，后续采用保留解释完整性的局部修正方式。v0.1.0 Tag 与 Release 保持原基线，后续阶段与新版本发布安排以作者指令为准。

## 参考资料

- [Modular Handbook 目录](https://handbook.modular.com/llms.txt)：用于比较主题覆盖与本书边界。
- [Key metrics for LLM inference](https://handbook.modular.com/llm-inference-basics/llm-inference-metrics/)：参考用户时延、输出间隔与吞吐的区分；具体计时口径按本书实际 API 核对。
- [LLM performance benchmarks](https://handbook.modular.com/inference-optimization/llm-performance-benchmarks/)：参考目标负载、质量与性能分开评估的方法。
- [LLM observability](https://handbook.modular.com/infrastructure-and-operations/comprehensive-observability/)：参考指标与日志联合分析的方法，转化为目标设备上的测量方案。
