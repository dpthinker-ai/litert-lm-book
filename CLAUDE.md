# CLAUDE.md — 《端侧大模型推理：原理与 LiteRT-LM 实现》写作规范

本目录是书稿工作区。全稿已成，当前处于修订期；全书定位、目录与开放项见 `BOOK_PLAN.md`。本规范逐字加载进每个会话——每条都是检查项，不是口号。

**工作区锚点**（新会话开工前先确认）：
- LiteRT-LM 源码位于 `/Users/dpthinker/workspace/LiteRT-LM`；全书锁定 tag **v0.13.1**（已冻结）。核对代码用独立 worktree `/Users/dpthinker/workspace/LiteRT-LM-v0.13.1`，不扰动 main 工作区
- 术语表 `appendix/glossary.md` 即附录 A 正文，新术语随改动同步维护
- 书稿仓库为私有；开源与否未决策前，不向任何外部渠道发布内容片段

## 一、总纲（作者拍板的六条，不可妥协）

1. **用词严谨规范**：术语统一、断言有据、数字有源。
2. **拒绝夸大**：不写博眼球的话；性能表述永远带条件与出处。
3. **通俗易懂**：每个概念先给直觉再给严格定义；读者聪明，但不全知。
4. **逻辑严格遵从代码**：书中描述的每个机制，必须是 LiteRT-LM 代码里真实存在的样子；与代码冲突时改书不改事实。
5. **文字像人写的**：按第五节的方法论写作与修订；改写后的文字必须过"除 AI 味"审校。
6. **图表到位**：该配图的地方必须配图（一律 SVG），该用表的地方用表（第六节）。

## 二、严谨性制度（对应总纲 1、4）

**断言四级制**（写作工具，用于自检，不暴露给读者）。正文每个技术断言必须能归入一级，归不进的删掉：
- 【实证】能落到源码位置（v0.13.1，由代码块出处注释或附录 B 代码地图承载）或本书基准数据集（附录 D）→ 直接陈述
- 【文档】来自官方文档、博客、论文、硬件资料或 issue → 在支撑断言后加语义化页下注，脚注写全机构或作者、标题、日期或版本（可得时）、链接与访问日期；HTML 可点击，PDF 将脚注排在引用所在页底部。正文不用“出处见附录 X”等间接指引，X 指任意附录。GitHub issue 正文统一写作 `LiteRT-LM#2568`。本项目脚注只用于来源引用；补充解释写入正文或侧栏。正文脚注已经引用的来源不得在任何附录重复列出，附录之间也不得重复；附录只保留正文与其他附录均未引用、且对该附录确有用途的来源。“实验数据见附录 D”“复现命令见附录 C”等本书内部内容交叉引用可以保留
- 【常识】教科书级公认内容（目标读者的大学课本或经典综述可查）→ 直接陈述；但凡带具体数字或存在争议，降级按【文档】处理
- 【推测】基于代码的合理推断 → 用"我们推测/据此推断"显式标注

**代码引用与代码块**（2026-08-31 作者裁定，覆盖旧"正文写 file:line"格式）：
- **正文不出现文件路径与行号**；正文至多出现函数名、类名、CLI 参数等读者用得上的名字，无代码块支撑的段落用职责与机制语言行文，不串标识符
- 出处写在**代码块首行注释**（C++/Kotlin/Swift/JS 写 `// runtime/core/tasks.cc:435`，Python/Shell 写 `# python/litert_lm/engine.py:129`）；全书统一锚定 v0.13.1，由前言声明。附录（代码地图、复现、数据集、答案）作为查阅材料可保留行内引用
- **代码块克制、按需**：只在机制非代码不能说清、或断言必须当场取证时才贴；遵守"问题→代码→结论"三明治（块前有它回答的问题）；片段 ≤30 行，只留关键行
- 贴出的代码与 tag 版本逐字一致。允许且仅允许三种插入：块首出处注释、省略处的省略标记、行尾编号标记（讲解写在正文，不写进引文）；注释符号随宿主语言；不加其他注释，不翻译源码内英文注释
- 对其他项目的代码引用（对照视野等）：正文同样不写路径行号，出处以语义化脚注注出（仓库、文件与行、版本）
- **引用必须核验**：新增或改动的每处出处注释，落笔前逐条 Read 该行，确认内容确实支撑断言；引用存在但不支撑，按核查失败处理

**数字纪律**：
- 一切性能数字标明条件：设备、模型、量化、后端、上下文长度；只引用附录 D 基准数据集与注明出处的官方数据
- 能算的账当着读者面算，保证读者可验算。单位统一：内存 GiB、吞吐 tokens/s、时延 ms、带宽 GB/s（首次出现注明按 10^9 计）；公式符号首次出现时定义，代入数值必带单位；数字用半角阿拉伯数字

**「对照视野」侧栏**（评述 llama.cpp / MLC-LLM / ExecuTorch 等）：
- 每个 ≤半页；对其他项目的断言，【实证】须给 `仓库名/文件:行 @ 版本`，给不出则按【文档】注出处；不适用【推测】——不对别人的代码做推测
- 只陈述"不同的选择与各自的权衡"，不下"谁更好"的裁决；禁词表与数字纪律在侧栏内同样生效

## 三、反夸大规范（对应总纲 2）

- **禁词表**（书稿正文与标题一律不用）：颠覆、革命性、炸裂、王炸、吊打、秒杀、遥遥领先、天花板、终极、震撼、必看、干货、硬核（作形容词）、赋能、抓手、闭环（作动词）、完美、极致
- 倍数表述必须完整：正文写"在〈设备/模型/条件〉下 decode 吞吐从 a 到 b（约 3 倍，出处）"。标题与章首导语可用简式（"快 3 倍"），但正文首次展开处必须立即给出完整表述
- 形容词让位于数据：出现"显著/大幅/极大"时自查能否换成数字；换不成就不下这个判断
- 承认局限是本书风格：无真机验证的部分（如 NPU）明确标注"基于代码分析"；上游真实缺陷（issue）照写不讳

## 四、通俗性规范（对应总纲 3）

- **概念三步走**：一句直觉（人话）→ 严格定义 → 在 LiteRT-LM 里的对应物（文件/类名）
- **类比要收敛**：用了类比，必须交代类比在哪里失效；一个概念至多一个类比；避开 AI 惯用的陈腐类比（图书馆、高速公路、管家之类），类比尽量取自工程师的真实经验
- **一节新引入的术语表级术语 ≤3 个**（自查方式：数本节新增 `appendix/glossary.md` 条目数）；超过即拆节或后移
- 术语首次出现给中英对照并录入术语表，此后全书统一（例：全书用 prefill、KV cache，不用"预填充""键值缓存"）。"首次出现"以**成书章节顺序**为准
- **术语与语气纪律（严谨、认真、严肃、通用；对应总纲 1，硬性）**：
  - 技术概念一律用业界通用术语（prefill、KV cache、speculative decoding、roofline、memory-bound / compute-bound、mmap、quantization、tokenizer…）；中文取约定俗成译法或直接用英文。**不自造黑话、不生造缩写、不用只有本书懂的说法**。
  - **教科书体：自造的叙事标签一律退役，以通用术语为主表述**。"三堵墙"→内存容量约束 / 内存带宽约束 / 功耗与异构约束；"一个 token 的一生"→推理流水线（从输入到输出的完整链路）；decode"心跳"→decode 的一次前向—采样迭代（decode step）；"凿墙"→（针对某约束的）优化。这类记忆性说法至多在对应通用术语首次出现处以一句括注带过（如"内存带宽约束（后文有时称带宽墙）"），不作为章节标题、小节标题或严谨断言的主词，不进术语表。图内文字、图注、章节标题同步改为通用术语。
  - **口语化、俏皮、网络化表达一律改为准确严肃的技术语言**：如"挤牙膏""白赚""攥在手里""这副眼镜""口水话""吐字"之类，替换为精确描述（逐 token 串行生成 / 净收益为正 / 暂存不输出 / 这一分析视角 / 冗余内容 / 输出 token）。
  - 语气：面向中高阶工程师的严肃技术书，不卖萌、不调侃、不抖机灵；比喻服从第五节"类比要收敛"，且永不取代精确定义。审校时把本条与除 AI 味清单一并执行。
- 目标读者画像：会 C++、懂 Transformer 基础；不解释指针，但要解释 signature

## 五、写作方法论与除 AI 味（对应总纲 5）

**方法论基准**：
- 平克《风格感觉》的古典风格：写作是"指着世界给读者看"，作者与读者平等；不炫知识量
- 奥威尔六原则 / Strunk & White：见惯的比喻不用；删掉不承担信息的词；短词优先；主动语态优先；以及第六条——**任何规则，宁可打破也不写出别扭的句子**（本节清单同样适用此逃生阀，防止机械执行出新的套路味）
- 中文技术写作惯例：中英文之间留半角空格；全角标点；专名不硬译

**除 AI 味自查清单**（适用于所有新增或改写的文字，硬性 pass）：
1. 删空洞总结句（"总而言之，X 扮演着至关重要的角色"→ 删，或换成具体结论）
2. 删填充词：值得注意的是、需要指出的是、不难发现、显而易见、可以说、本质上、毫无疑问、众所周知、让我们、综上所述、（句首的）事实上；"换句话说"一章至多一次
3. 拆排比瘾：连续"不仅…而且…更…"、机械的"首先/其次/最后"→ 按逻辑自然行文
4. 砍轻动词与万能名词："对 X 进行初始化"→"初始化 X"；"实现了性能的提升"→ 给数字；"机制/维度/层面/体系"只在确有所指时用
5. 拆同义堆叠：成对近义修饰（"高效可靠""强大而灵活"）删掉一半，或换成一个事实
6. 查翻译腔："被广泛认为""作为一种…""在很大程度上""…的存在"、三层定语链（"一个基于 X 的用于 Y 的 Z"）→ 拆成中文短句
7. 句式求变：连续几句全是齐整的主谓宾即嫌疑；中文允许省主语、允许三五个字的短句，用起来
8. 列表节制：bullet 列表每节至多 1 处，只用于真正并列的枚举；能叙述的不列表
9. 段落有长短节奏：连续三段字数差均在 15% 以内视为等长，改写其中一段
10. 一章只在必要处总结一次，不许每段末尾都带"小结句"
11. 章首第一句就进入具体问题，禁"在人工智能飞速发展的今天"式空转
12. 破折号、冒号节制：破折号一段至多一处（成对的插入语破折号算一处）
13. 出现抽象形容词处自查能否具体化（数字、文件名、现象）
14. **独立审校**：抽改动所在节的第二段与末段检查——a) 有无单句超 50 字仍未断句；b) 相邻三句是否同一句式开头；c) 段内是否至少含一个具体锚点（数字/文件名/现象）。此项由 `book-review` skill 或独立审校会话执行，禁止撰写该章的同一会话自评

**humanizer 补充项**（据 [blader/humanizer](https://github.com/blader/humanizer) 的 31 类 AI 痕迹，取对中文技术书适用者，与上表并用）：
15. 破折号从严：优先不用；确需时全段至多一处，且**不用破折号堆叠插入语**——凡能用冒号/句号/逗号表达的一律替换（humanizer 对英文是"全删"，中文按"能不用就不用"）。
16. **不用"加粗小标题＋冒号"当列表骨架**（`**X**：…` 四连即模板味）；正文加粗克制，只落在真正的关键词上，不整句加粗。
17. 忌否定式排比"不只是 X，而是 Y""与其说…不如说"当口头禅；忌虚假范围"从 X 到 Y"作泛化填充；忌强凑三项排比。
18. 忌权威腔与设问套路："本质上""说到底""究其根本""真正的问题在于""值得深思""不难看出"。
19. **描述现状，不写改动流水账**：讲代码"是什么、怎么运作"，不写"我们把 X 改成 Y""这里做了优化"式叙述；上游版本演进只进「版本注记」侧栏。
20. 忌制造戏剧性与格言式收尾；结论落在具体事实、数字、文件名上，不落在金句上。

**审校执行**：语言审校用 `.claude/skills/book-review`（每次一章、就地修正，含禁词表、退役标签与叙事姿态检查）；第 14 条由该 skill 或独立会话执行。新写与改写段落的正面手法（章节骨架、开篇/过渡/收尾、算账、对比组织、类比纪律）参考 `.claude/skills/writing-reference`，取自两本参照书的全书精读，含与本规范的冲突裁决表。作者逐条批注实战沉淀的问题类型学、修法库与协作规范见 `.claude/skills/review-calibration`（源自前言与第 1 章审阅），逐章审校、润色与处理批注前一并加载。

**好/坏对照（校准示例，新会话先读这三组）**：
- ✗ "KV cache 是一项至关重要的优化技术，它极大地提升了推理性能。"
- ✓ "不缓存 K/V，每生成一个 token 就要对前面全部 token 重算一遍注意力——第 1000 步要付 1000 份冤枉计算。KV cache 用内存换掉了这笔重复账。"

- ✗ "LiteRT-LM 采用了先进的双缓冲机制，完美解决了 GPU 读写冲突问题。"
- ✓ "部分 GPU 后端不允许对同一块缓冲同时读写。LiteRT-LM 的办法很朴素：备两套缓冲，读旧写新，然后交换指针（`llm_litert_compiled_model_executor.h:329 @ v0.13.1`）。"

- ✗ "首先，Engine 负责模型资源的加载与管理；其次，Session 负责维护对话上下文；最后，两者相互配合，构成了一个高效、灵活的推理架构。可以看到，这种分层设计是非常优雅的。"
- ✓ "Engine 管模型，Session 管对话。分开的好处，头文件注释里的例子一眼能看穿：session1 先 prefill \"What is the tallest building \"，Clone 出 session2，一边接 \"in the world?\"、一边接 \"in France?\"——公共前缀只算一次（`runtime/engine/engine.h:238 @ v0.13.1`）。"

## 六、图表规范（对应总纲 6）

- **何时配图**：流程、结构、数据流、状态变迁——文字描述超过一段仍难在读者脑中成像的，必须配图。**何时用表**：方案权衡、参数对照、口径对比等真正并列的信息用表；表不用于叙事
- 不为凑数配图；脊柱与攻坚章（第 3-9 章）2-3 幅图 + 至少 1 张权衡表、起点章 1-2 幅的预算已兑现，新增图表沿用此密度
- **图一律 SVG 手绘**：CSS 变量画法，变量表在本仓 `assets/book.css`（单一事实源）；每张 SVG 在 `<defs><style>` 内给变量 fallback 实色（如 `var(--accent, #4f9cff)`），保证脱离站点样式与 PDF 渲染时不失色；图内文字用 SVG text（可检索），术语与正文一致
- **编号与图注**：编号"图 N-M / 表 N-M"（N 为章号）；图注一句话说清读者应从图里看出什么，不复述正文；禁"架构示意图"式空图注
- 截图仅限终端输出实录；其余一律 SVG 重画

## 七、修订工作流

全稿已成，一次修订的循环就四步，不再维护 review.md 审校留痕，也没有 BOOK_PLAN 章卡需要同步：

1. **先核对再动笔**：涉及代码行为的改动，先在 `/Users/dpthinker/workspace/LiteRT-LM-v0.13.1` 读代码；新断言按四级制归级，引用行逐条 Read 核验
2. **改正文**：新增或改写的文字过第三、四、五节清单；新术语录入 `appendix/glossary.md`（附录 A）
3. **提交前自检**：`bash scripts/lint_prose.sh <改动文件>` 零命中；`bash scripts/build_book.sh` 无警告；改了章标题、术语或结构时，同步核对 SUMMARY.md、`scripts/finalize_pdf.py` 的 UNITS 表与 SVG aria-label（联动清单见 book-review skill 滤网五）
4. **git commit 即修订记录**：提交说明写清改了什么、为什么（含事实修订的依据）；不另写审校文档

整章级语言审校调用 `book-review` skill（每次一章）。发布 PDF 前跑一遍 `scripts/check_*.py` 全套。

## 八、文件与格式约定

- 正文：`chapters/chNN-slug/{chapter.md, figs/}`、`preface.md`、`parts/`、`appendix/`、`SUMMARY.md`；实验脚本入 `experiments/`，附录 C 汇总复现命令，基准数据口径以附录 D 为准
- `archive/` 为初稿期工作文件的冻结归档（备料、审校记录、模块底稿），仅作历史参考，不再更新
- 章节交叉引用统一写"见第 N 章 / 见 N.M 节"，不引用页码；前向引用须点明该处解答什么问题（例："双缓冲的实现细节见 6.3 节"），禁止空指的"后文详述"
- 行内公式写 `\(...\)`（mdBook 源文件写 `\\(...\\)`），块级写 `$$...$$`，不用 `$...$`
- 代码锚定 tag 由前言声明，全书统一 v0.13.1；上游演进写入「版本注记」侧栏，不改正文
