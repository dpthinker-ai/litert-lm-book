# 前言

市面上讲大模型的书，多在讲训练：怎么设计架构、怎么喂数据、怎么调超参。可当一个训练好的模型要跑起来，尤其是跑在你口袋里那部手机上时，真正的挑战才开始出现。电池供电、内存紧张、芯片一发热就必须降频——这样的设备，怎么把一个几十亿参数的模型跑得又快又稳定？这本书讲的就是这件事。

## 这本书讲什么

本书围绕 **LiteRT-LM** 展开。它是 Google 主推的端侧大模型运行时，已部署在手机、Chrome、Chromebook Plus、Pixel Watch 等出货产品中。作为 Google 在端侧 AI 方向的核心基础设施，它值得深入研究和持续关注——它有可能成为端侧大模型推理的重要参考实现。

但这本书不是 LiteRT-LM 的使用手册（官方文档已经写得很好），也不是它的代码逐行注释。它的视角是**实现与权衡**：为什么采用这个设计而非其他方案，每种设计在哪些约束下成立、又在哪些场景中遇到瓶颈。读完之后，你带走的不是"LiteRT-LM 怎么用"，而是"端侧推理这门系统工程是怎么回事"——换一个运行时，这些道理大多仍然成立。

需要说明的是：这是一本**非官方**的深度解析，并非 Google 官方出品，书中观点与任何错漏均由作者负责。

## 一条主线：推理流水线

全书有一条主线——完整的推理流水线：一段输入从进入模型到逐 token 产出的全过程。

你向模型输入一句话，它逐 token 返回。这中间发生了什么？输入文本如何变成 token 序列，如何经 prefill 进入模型，模型又在每个 decode step 产出一个 token，直到触发停止条件。理解这条链路之后，后面所有的细节才能找到各自的位置。这本书有意先带你走完这条主线（第二篇），再回头逐个深挖沿途的子系统（第三篇起）——因为理解一个系统，总是先需要一个整体的脉络，再填充分析的细节。

## 写给谁

这本书假设你会 C++（读得懂模板和智能指针）、了解 Transformer 的基本原理（理解注意力机制）。它假设你懂这些，所以不解释它们；但它会仔细解释什么是 KV cache 的双缓冲、什么是推测解码的接受率。

如果你是想把端侧 AI 集成到产品中的工程师，或是想进入推理系统方向的研究生，这本书是为你写的。如果你只想调 API 跑模型，官方文档更快；但当你的模型在某个设备上部署失败、或者性能远低于预期时，这本书会告诉你该往哪一层去查原因。

## 怎么读

- **按顺序读**最省力：第 1 章用三类物理约束引出问题，第二篇沿推理流水线建立主线，第三篇逐项分析各约束下的优化，第四篇（能力与工程）向外扩展。
- **第 2 章有一份「二十个问题」清单**，是全书的向导。读到答案时回头勾掉，能帮你随时知道自己在整张地图上的位置。
- **不必先会编译**。第 2 章教你用一条命令把模型跑起来；想读代码时，书里的关键机制都标了源码位置（引用体例见下一节），可以对着源码看。

## 关于代码引用与数字

**代码锚点**：全书的 LiteRT-LM 代码引用统一锁定在版本 `v0.13.1`，正文写成 `runtime/core/tasks.cc:413` 的简洁形式，不逐处重复版本号；对其他项目的引用则显式标注版本（如 llama.cpp 的 `@ b9873`）。代码之外的来源（官方文档、对照项目、学术文献、硬件资料）集中在附录 F。上游在持续演进，锁版本是为了让书里的每一处引用都能对得上——无论是现在还是将来。后续版本的演进，以「版本注记」侧栏补充，不改动正文。

**性能数据**：书里的理论上限（比如那条 decode 上限公式）都在正文中逐步推导出来，你可以自己验算。实测数据则来自一套固定的基准数据集（同一台机器、同一个模型 Gemma 4 E4B，方法与全部数据见附录 D），所有标注「〔基准 D〕」处都出自这套数据。纸面推算与真机实测，两者不混淆。书中也标明了哪些部分仅有代码分析、未经真机验证（如某些 NPU 细节）。
