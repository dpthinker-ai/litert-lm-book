# 前言

市面上讲大模型的书，多在讲训练：怎么设计架构、怎么喂数据、怎么调超参。可当一个训练好的模型要跑起来，尤其是跑在你口袋里那部手机上时，真正的挑战才开始出现。电池供电、内存紧张、芯片一发热就必须降频——这样的设备，怎么把一个几十亿参数的模型跑得又快又稳定？这本书讲的就是这件事。

## 这本书讲什么

本书的解剖对象是 **LiteRT-LM**，Google 的端侧大模型运行时。它的结构可以一句话说完：在 LiteRT（原 TFLite）之上为 LLM 专门打造的一个编排层——向上是 Engine/Session API 与一层 C ABI（Python、Kotlin、Swift、Web 的绑定从这里长出），中间是把一次请求编排成 prefill/decode 的核心调度，向下经编译模型调度到 CPU、GPU、NPU，模型则以单文件 `.litertlm` 分发。

选它做标本，有两个理由。其一，它是投产代码，不是论文原型：Chrome、Chromebook Plus、Pixel Watch 等出货产品里有它，官方示例应用与 Android 侧的模型部署路径也都基于它（出处见附录 F）——它的许多设计目标是千差万别的真实设备，而不是 benchmark 分值。其二，它把端侧推理的关键问题集齐了：KV cache 双缓冲、GPU 片上采样、量化与 mmap 冷启动、推测解码、多模态输入、约束解码与工具调用、LoRA 适配，每一项都能读到生产级的真实做法。

这本书不是它的使用手册（官方文档已经写得很好），也不是代码逐行注释。它的视角是**实现与权衡**：为什么采用这个设计而非其他方案，每种设计在哪些约束下成立、又在哪些场景中遇到瓶颈。读懂这一套，你带走的是"端侧推理这门系统工程是怎么回事"——换一个运行时，要解的还是这些问题，这些道理大多仍然成立。

需要说明的是：这是一本**非官方**的深度解析，并非 Google 官方出品，书中观点与任何错漏均由作者负责。

## 一条主线：推理流水线

全书有一条主线——完整的推理流水线：一段输入从进入模型到逐 token 产出的全过程。

你向模型输入一句话，它逐 token 返回。这中间发生了什么？输入文本如何变成 token 序列，如何经 prefill 进入模型，模型又在每个 decode step 产出一个 token，直到触发停止条件。理解这条链路之后，后面所有的细节才能找到各自的位置。这本书有意先带你走完这条主线（第二篇），再回头逐个深挖沿途的子系统（第三篇起）——因为理解一个系统，总是先需要一个整体的脉络，再填充分析的细节。

## 写给谁

这本书假设你会 C++（读得懂模板和智能指针）、了解 Transformer 的基本原理（理解注意力机制）。它假设你懂这些，所以不解释它们；但它会仔细解释什么是 KV cache 的双缓冲、什么是推测解码的接受率。

如果你是想把端侧 AI 集成到产品中的工程师，或是想进入推理系统方向的研究生，这本书是为你写的。读完后你应当能做到三件事：在脑中完整走通一次生成请求，从 `generate()` 到逐字产出的每一步发生了什么；用系统工程师的方式度量与解释端侧推理性能，为什么是这个数字、瓶颈在哪一类约束；看懂并有能力修改一个生产级推理运行时，加后端、加采样策略、或者修一个真实的 issue。如果你只想调 API 跑模型，官方文档更快；但当你的模型在某个设备上部署失败、或者性能远低于预期时，这本书会告诉你该往哪一层去查原因。

## 怎么读

- **按顺序读**最省力：第 1 章用三类物理约束引出问题，第二篇沿推理流水线建立主线，第三篇逐项分析各约束下的优化，第四篇（能力与工程）向外扩展。
- **第 2 章有一份「二十个问题」清单**，是全书的向导。读到答案时回头勾掉，能帮你随时知道自己在整张地图上的位置。
- **不必先会编译**。第 2 章教你用一条命令把模型跑起来；想读代码时，书里的关键机制都标了源码位置（引用体例见下一节），可以对着源码看。

## 关于代码引用与数字

**代码锚点**：全书的 LiteRT-LM 代码引用统一锁定在版本 `v0.13.1`，正文写成 `runtime/core/tasks.cc:413` 的简洁形式，不逐处重复版本号；对其他项目的引用则显式标注版本（如 llama.cpp 的 `@ b9873`）。代码之外的来源（官方文档、对照项目、学术文献、硬件资料）集中在附录 F。上游在持续演进，锁版本是为了让书里的每一处引用都能对得上——无论是现在还是将来。后续版本的演进，以「版本注记」侧栏补充，不改动正文。

**性能数据**：书里的理论上限（比如那条 decode 上限公式）都在正文中逐步推导出来，你可以自己验算。实测数据来自两套分开标注的基准：主基准（一台 Mac，Gemma 4 E4B）与扩展基准（一台 Qualcomm 手机，自编译二进制），方法与全部数据见附录 D；所有标注「〔基准 D〕」处都出自这套数据，纸面推算与真机实测不混淆。仅有代码分析、未经真机验证的部分（如 NPU 的执行行为），书中都就地标明。
