# 前言

市面上讲大模型的书，多在讲训练——怎么设计架构、怎么喂数据、怎么调超参。可当一个训练好的模型要真正跑起来、尤其是跑在你口袋里那部手机上时，故事才刚开始。那台没有插电、内存紧张、芯片一忙就发烫的设备，怎么把一个几十亿参数的模型伺候得又快又稳？这本书讲的就是这件事。

## 这本书讲什么

我们选了一个真实的、投产的标本来解剖：**LiteRT-LM**，Google 开源的端侧大模型运行时。它跑在 Chrome、Chromebook Plus、Pixel Watch 这些真实出货的产品里，代码公开可读。拿它当标本的好处是，每一个设计决策都能落到具体的文件和行号上，而不必停留在"业界一般这么做"。

但这本书不是 LiteRT-LM 的使用手册（官方文档已经写得很好），也不是它的代码逐行注释。它的视角是**实现与权衡**：为什么是这个设计而不是别的，它在哪堵墙前低了头、又在哪里凿出一条路。读完之后，你带走的不该只是"LiteRT-LM 怎么用"，而是"端侧推理这门系统工程是怎么回事"——换一个运行时，这些道理大半还成立。

需要说明：这是一本**非官方**的深度解析，作者与 Google 无关，书中观点和可能的错漏都由作者负责。

## 一条主线：推理流水线

全书有一条主线，就是第二部所讲的：完整的推理流水线——一段输入从进入模型到逐 token 产出的全过程。

你向模型输入一句话，它逐 token 返回。这中间到底发生了什么？输入文本如何变成 token 序列，如何经 prefill 进入模型，模型又如何在每个 decode step 产出一个 token，直到触发停止条件。把这条链路走通，你就有了一张能挂住后面所有细节的骨架。这本书刻意先带你走完这条主线（第二部），再回头逐个深挖沿途的子系统（第三部起）——因为人理解一个系统，总是先要一个能叙事的整体，再填分析的细节。

## 写给谁

这本书假设你会 C++（读得懂模板和智能指针）、懂 Transformer 的基本原理（知道注意力是怎么回事）。它不会解释什么是指针，但会仔细解释什么是 KV cache 的双缓冲、什么是推测解码的接受率。

如果你是想把端侧 AI 用进产品的工程师、或想进入推理系统方向的研究生，这本书是为你写的。如果你只想调个 API 跑个模型，官方文档更快；但当那个模型在某个设备上跑不动、或慢得出奇时，这本书会告诉你该往哪一层去看。

## 怎么读

- **按顺序读**最省力：第 1 章用三类物理约束立起问题，第二部沿推理流水线建立骨架，第三部逐项分析各约束下的优化，第四部（能力与工程）向外扩展。
- **第 2 章有一份「二十个问题」清单**，是全书的向导。读到答案时回头勾掉，能帮你随时知道自己在整张地图的哪里。
- **不必先会编译**。第 2 章教你用一条命令把模型跑起来；想读代码时，书里每个关键引用都标了 `文件:行号 @ 版本`，可以对着源码看。

## 关于代码引用与数字

**代码锚点**：全书的 LiteRT-LM 代码引用统一锁定在固定版本 `v0.13.1`，正文写成 `runtime/core/tasks.cc:413` 这样的简洁形式，不逐处重复版本号；对其他项目的引用则显式标注版本（如 llama.cpp 的 `@ b9873`）。上游是活的、一直在变，锁版本是为了让书里的每一处引用今天、明天都对得上；后续版本的演进，会以「版本注记」侧栏补充，而不改动正文。

**性能数字**：书里的性能上限（比如那条 decode 上限公式）都是当着你的面算出来的，你可以自己验算。而实测数字则来自一套固定的基准数据集（同一台机器、同一个模型 Gemma 4 E4B，方法与全部数据见附录 D），凡标注「〔基准 D〕」处均出自这套数据——我们不把纸面推算和真机实测混为一谈。书中还诚实标注了哪些部分（如某些 NPU 细节）只有代码分析、没有真机验证。

下一页，从端侧推理的三类物理约束开始。
