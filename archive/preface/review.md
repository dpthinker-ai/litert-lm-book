# 前言修订记录：LiteRT 与 LiteRT-LM 的关系

日期：2026-08-15

## 范围

- 保留 LiteRT-LM 与 LiteRT 的层级、职责边界和主调用链。
- 按作者反馈撤回前言中的 llama.cpp、MLC-LLM、ExecuTorch 横向对比；第 1 章原有的同类项目对照不在本次修订范围内。

## 事实核查

| 断言 | 级别 | 依据 |
|---|---|---|
| LiteRT-LM 是使用 LiteRT 运行 LLM 的编排层 | 文档 | Google AI Edge，LiteRT-LM Overview，访问日期 2026-08-15 |
| LiteRT 的 `CompiledModel` 负责模型编译和同步/异步调用 | 文档、实证 | LiteRT overview；`runtime/executor/llm_litert_compiled_model_executor.cc:1648-1654`、`:729-734`、`:941-944` |
| Engine 创建 LiteRT 环境和 compiled-model executor | 实证 | `runtime/core/engine_advanced_impl.cc:279-288` |
| `GPU_ARTISAN` 不走同一 compiled-model 路径 | 实证 | `runtime/engine/engine_factory.h:202-206` |

以上源码锚点均按 LiteRT-LM v0.13.1（`a0afb5a56acd`）逐行核验。

## 审校与构建

- [x] 正文未保留内部断言等级标签。
- [x] 外部来源使用语义化页下注，未与附录来源重复。
- [x] `bash scripts/lint_prose.sh`：0 命中。
- [x] `python3 scripts/check_citations.py`：通过，51 个脚注定义、64 处引用。
- [x] 源码锚点检查：644 个唯一锚点通过。
- [x] HTML 构建通过，27 页内 1366 个本地引用通过检查。
- [x] PDF 构建通过，共 202 页、151 条书签；前言关系表、图 前-1 和页下注已渲染检查。

## 结构调整记录

- `2026-08-16 · v4.9`：本文件所述的「LiteRT 与 LiteRT‑LM 的关系」一节整体移入第 1 章 1.1 节（含软件栈分工表、主调用链图与两条页下注；表、图分别重编号为表 1-1、图 1-1，脚注改名为 `[^ch01-litertlm-overview]`、`[^ch01-litert-overview]`）。本节事实核查结论随文迁移，不再由前言承载；前言新增一句指向 1.1 节的阅读指引。移动后前言重新过 lint 与引用检查（见主会话 2026-08-16 记录）。
- `2026-08-16 · v4.9（第二轮，连贯性修订）`：按作者审阅意见重写开头两段（删除 AGI 口号、重复论点和"借书学习"动机，改从具体现象切入）；端侧部署四条理由压缩为一句并指向第 1 章 1.2 节；「Google 的端侧布局」末段删去与「这本书讲什么」重复的"代码公开"论证并去掉"优质样本"提前结论；「推理流水线」补上第一部分的作用（原文缺失，与"阅读路径"的"各部分的作用见前文"不闭合）；零星修正——主基准表述改为以 Mac 为主语、删除空洞的"变化之快"句、"缺乏中文书"断言加检索限定、删去开发者大赛奖金细节、主基准模型句去重。
- `2026-08-16 · v4.9（第三轮，残余去重）`：按作者指正，前言"端侧部署的独特价值来自四个方面：数据边界、网络时延、服务成本与离线可用性"一句仍与第 1 章 1.2 节四条 bullet 重复，改为不枚举的纯指引（"体现在哪些方面、有什么边界与代价，在第 1 章 1.2 节展开"）；开头段"输入与输出不离开设备"与 1.2 数据边界条目同义，一并删除。四条部署理由的完整表述现只在第 1 章 1.2 节出现一次。
- `2026-08-16 · v4.9（第四轮，对照完整性）`：按作者指正，两个时间节点的对照中 Llama 2 未给出参数规模，与后文 Gemma 4 对应不起来。已为 Llama 2 补"7B、13B、70B 三档参数"（【文档】，Meta 发布博客，既有脚注 `[^preface-llama2]`），并为 E2B 补"2B 有效参数"（与本书 E4B=4B 有效参数口径一致）。
- `2026-08-16 · v4.9（第五轮，收敛表述）`：按作者指正，删除"差距正在缩小，而非扩大"的过度结论（两个时间节点的一次对照只能支撑"能力组合大致相当"的静态比较，不足以断言差距趋势）；"端侧的追赶不只依赖硬件与量化进步"改为"不只靠硬件，也靠模型形态本身的变化"（量化在前文未展开为追赶驱动因素，突兀；模型形态变化由 Karpathy 两条表态承接）。
- `2026-08-27 · v4.9（第六轮，来源与事实修订）`：E2B 端侧数据改引 Google Developers Blog 一手来源（原转述来源的"约 1.4 GiB"修正为官方"不足 1.5 GB"口径，吞吐数字补 decode 标注），删除 Edge AI and Vision Alliance 转述脚注；Gemma 4 榜单名次补"Arena AI 文本榜单"条件；Karpathy 两条表态由 bullet 列表改为叙述体；能力承诺中的 `generate()` 修正为 `GenerateContentStream`（v0.13.1 无小写 generate 公开接口，`runtime/engine/engine.h:112-134`）；方法论段（`litertlm_print` 与证据链）自「推理流水线」移入「这本书讲什么」；"阅读路径"的模糊指引"各部分的作用见前文"改为指向「推理流水线」一节。
- `2026-08-27 · v4.9（第七轮，整体连贯性）`：按作者反馈对前言做结构性梳理，以挪动与合并为主，散文句子基本原样保留。开头补一句路标，预告后两节的两块背景；「云侧与端侧模型」删除与开头及时间节点 bullet 三重重复的首段——Mac 基准句（decode 数十 token/s）移入「关于代码引用与数字」主基准描述，TinyLlama 端侧引证并入 Llama 2 bullet，"尚无中文系统性专著"断言移入「这本书讲什么」作开篇并与"本书尝试补上这个空档"衔接；「对这个问题的回答，很大程度上取决于端侧部署的独特价值」与下段指针合并为单句过渡；「Google 的端侧布局」开头补承接句（模型之外还需平台），"全栈掌控"论点自段尾提至段首；原「这本书讲什么」中"选择 LiteRT-LM 的两个原因"整段移至「Google 的端侧布局」研究样本段之后（改冠句"作为分析对象，它还有两个务实的优点"），与样本论证合流，消除两处重复的技术清单之一。脚注定义与引用数量不变（14 条，均一处引用）。
- `2026-08-27 · v4.9（第十轮，外部润色合并）`：作者以 DeepSeek 润色前言并定稿，本次核验后合入。润色的重组（开头三段并拆、Karpathy 两段合一、"正是这种全栈布局"拆句、方法论段"生产级"措辞等）经逐项核对：事实、数字、脚注锚点、【推测】标记与结构骨架均未受损，全部采纳。仅做两处规范对齐："浏览器——没有机房"破折号改回冒号（除 AI 味第 15 条，能用冒号一律替换）；"生产环境的端侧运行时"统一为"生产级端侧运行时"（与开篇及 BOOK_PLAN 口径一致）。lint、引用配对、一致性、链接检查四项复验通过。
- `2026-08-27 · v4.9（第九轮，开头第二句写实）`：按作者反馈，"输入几个字，回答逐字流出"不符合真实使用习惯（实际输入常为长代码、文档，且有多轮往来）。改为"提问、贴代码、传文档，一来一回好几轮，答案逐字出现在屏幕上"：输入侧换作三类常见场景并点明多轮；输出侧保留流式事实（呼应全书逐 token 生成主题），动词落回常规搭配，并以"屏幕"一词接住下段"屏幕背后是数据中心"。lint 复验 0 命中。
- `2026-08-27 · v4.9（第八轮，去 AI 味微调）`：对第七轮改写引入的表述做除 AI 味收敛，六处——自造量词"两块背景"改为"两件事：端侧模型的能力走到了哪一步，Google 为此准备了什么"；翻译腔"一块以 GiB 计的内存"改为"几 GiB 的内存"；删除合并段落时带入的填充词"需要说明的是"；别扭搭配"两个务实的优点"改为衔接句"除了问题本身足够通用，选定它还有两个原因"，恢复与样本论证的逻辑连接；同词近距离重复"模型之外……把模型送上设备"改为"光有模型还不够，还需要一整套把它们送上设备的平台"；删冗余词"能力本身"中的"本身"，1.2 节指针补"会"字。无事实、数字与引用变动；lint 与引用配对复验通过。
