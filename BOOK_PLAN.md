# 《端侧大模型推理：原理与 LiteRT-LM 实现》规划书

> 当前状态：**v4.9 — 结构调整**（2026-08-16）。前言、11 章、尾声与附录 A-E 已成稿，共 21 张 SVG；代码统一核对 LiteRT-LM v0.13.1。
> v4.9 变更：前言「LiteRT 与 LiteRT‑LM 的关系」一节（含图、表与两条脚注）整体移入第 1 章 1.1 节；第 1 章改为先介绍 LiteRT-LM 与 LiteRT 的分工，再讲三类约束；原 1.1-1.5 顺延为 1.2-1.6，图 1-1/表 1-1 重编号为图 1-2/表 1-2，练习新增第 6 题。第二轮：前言连贯性修订（开头重写、端侧理由去重等）；第 1 章 1.6 删除与前言重复的部署记录与选择理由散文（保留对照表），修正 1.4 一处逻辑方向表述。第三轮：前言删除仍枚举四项部署理由的指引句与"输入与输出不离开设备"同义短语，四条理由仅保留在第 1 章 1.2 节。第四、五轮：时间节点对照补参数规模（Llama 2 三档、E2B 2B 有效参数）；收敛"差距缩小而非扩大"的过度结论；删去衔接生硬的"硬件与量化进步"表述。
> 本轮修订重点：prefill 取消与回调等待、MTP 聚合接受比例、Swift 生命周期案例、受控实验口径、NPU 证据边界，以及 mdBook/PDF 发布安全。
> 已完成或部分完成：Mac 主基准、Android CPU/GPU 扩展、CPU 线程扫描、MTP 多组负载、约束解码小样本，以及两台设备上的 NPU 部署探测；NPU 尚未完成 prefill 或 decode。
> 尚未完成并与附录 D 对账：编译缓存开/关的外部墙钟受控对照、同模型 int4/int8 对照、分段并行加载开关对照、Python/C++ 行为一致性、多模态端到端、双 tokenizer 对照，以及使用匹配模型产物、SoC 代际和运行时的 NPU 推理。
> 当前验收以 `archive/chapters/*/review.md`、附录 D 与构建检查为准。
> 书名（已定）：《端侧大模型推理：原理与 LiteRT-LM 实现》
> *On-Device LLM Inference: Principles and Practice with LiteRT-LM*
> 素材基础：`/Users/dpthinker/workspace/litert-lm-guide/data.json`（12 模块深度分析，约 20 万字）
> 代码锚点：`google-ai-edge/LiteRT-LM` **@ v0.13.1（已冻结，2026-07-05）**；核对代码在独立 worktree checkout 该 tag，不扰动 main 工作区。`AGENTS.md` 中的示例引用已按 v0.13.1 核验回填
> 写作规范：`AGENTS.md`；本规划与其冲突时，以 `AGENTS.md` 为准

---

## 〇、定位与范围

**这本书讲什么**：以 Google 的端侧 LLM 运行时 LiteRT-LM 为主要实现对象，
系统回答一个问题：**大语言模型如何在手机、手表和浏览器等受限设备上运行**。

**对读者的承诺**：读完本书，你能
1. 完整走通一次生成请求：从 `generate()` 到输出 token 的每一步发生了什么；
2. 用系统工程师的方式度量与解释端侧推理性能，包括数字口径与资源约束；
3. 看懂并有能力修改/扩展一个生产级推理运行时（加后端、加采样策略、修真实 issue）。

**不做的事**：
- 不写 API 使用教程（官方文档已覆盖；本书视角是"实现与权衡"）；
- 不搞论文综述式的优化技术罗列（只讲被这套代码验证过的、以及它为什么没选别的）；
- 不假装官方（书名与前言明确"非官方深度解析"）。

**定位判断**（内部表述，属【推测】级）：据我们 2026-07 的检索，尚未见到以生产级
端侧 LLM 运行时为主要实现对象的系统性中文书；llama.cpp 的公开解读多为碎片文章，
论文综述又不落地到工程实现。本书押注这个空档。

**体量约束（作者拍板）**：正文 + 附录控制在 **250-300 页**；11 章 + 尾声，
单章 16-28 页——章少而厚，每章仍必须是"一个问题"的完整解答。

---

## 一、四项奠基决策

### D1. 体例 【✅ 已拍板：混合路线 C】
每章由领域问题驱动，LiteRT-LM 作为"被深度研究的标准实现"，另设「对照视野」
侧栏简评 llama.cpp / MLC-LLM / ExecuTorch 的不同选择（DDIA 模式；
侧栏写法约束见 `AGENTS.md` 第二节）。

### D2. 读者起点 【✅ 已拍板：中阶工程师】
会 C++（读得懂模板与智能指针）、懂 Transformer 基本原理；不假设读过推理框架源码。

### D3. 发布形态 【✅ 已拍板：内部打磨先行，暂不开源】
私有仓库内部迭代，mdBook/Quarto 仅做本地/私有构建；先自己学习和打磨。
核心版（第 1-9 章）打磨满意后，再评估开源与出版路径。
现有学习网站降级为本书的"代码地图"附录与伴生参考站。

### D4. 语言 【✅ 已拍板：简体中文先行】
成稿后再评估翻译。

> ✅ 拍板记录：D3=内部打磨先行（2026-07-04）；D1=混合路线 C、D2=中阶工程师、
> D4=简体中文先行、书名=候选 A《端侧大模型推理：原理与 LiteRT-LM 实现》（2026-07-05）。
> 全部奠基决策拍板完毕；第二部分以推理流水线为主线。

---

## 二、结构设计的六条原则

1. **问题驱动，不是目录驱动**。每章标题必须能改写成一个读者真实会问的问题；
   写作前每章过"一句话使命"测试。
2. **主线先行**。先走通推理流水线，再分析各子系统。
3. **螺旋深化**。核心概念出现三次、一次比一次深（KV cache：第 2 章直觉 →
   第 4/5 章使用 → 第 6 章深潜）。
4. **每章可验证**。每章至少一个可复现实验；全书共用一套"基准数据集"
   （同一模型、同一设备，P1 一次性采集，见附录 D）；多个实验直接解释上游真实 issue。
5. **素材与正文分离**。每章 `notes.md`（素材）+ `chapter.md`（正文）+
   `review.md`（两个 pass 的留痕）；正文只贴 ≤30 行关键代码，必带 `file:line @ <tag>`。
6. **图表到位**（作者拍板）。流程/结构/数据流必配 SVG 手绘图（规范见 `AGENTS.md`
   第六节），对比信息用表；每章图表在章卡里预先规划，不为凑数配图。

**内容归并原则**：基础话题不独立成章，就近并入它发挥作用的地方：
线程/异步 → Prefill 章与后端章；会话状态(Clone/Checkpoint) → KV cache 章；
Roofline → 跑起来章 + KV cache 章开篇；LoRA → 模型形态章。
好处：话题出现在读者正需要它的上下文里；代价：内部版的评审粒度从"章"细化为"节"。

**版本锚定策略**：全书代码引用锁定一个 release tag；「版本注记」侧栏消化上游演进，不改正文。

---

## 三、目录 v4.9（四个部分 · 11 章 + 尾声 · 5 附录）

> 章卡字段：使命 / 核心问题 / 内容 / 图表 / 实证状态 / 素材 / 缺口
> 状态标记以当前 v4.8 成稿和附录 D 为准：“已完成”表示已有代码核验或实验记录；“尚未完成”对应附录 D 的开放项；“原规划”仅保留历史目标，不表示已经兑现。

### 前言 · 为什么读这本书

- 使命：读者在进入第 1 章前，理解为什么端侧推理值得系统学习、本书讲什么、怎么读。
- 内容：云侧与端侧模型能力的差距；Google 的端侧布局与选择 LiteRT-LM 的原因；本书范围与结构；阅读路径；代码引用与数字体例。
- 图表：无。原「图 前-1 主调用链 / 表 前-1 软件栈分工」随 LiteRT 与 LiteRT‑LM 关系一节移入第 1 章 1.1 节（v4.9 结构调整）。

### 第一部分 · 约束、指标与系统概览（2 章，约 40 页）

**第 1 章 端侧 LLM 与 LiteRT-LM：三类物理约束与运行时版图**（约 16 页）
- 使命：读者先明白 LiteRT-LM 是什么、它与 LiteRT 的分工，再亲手算出"4B 模型在手机上的理论 decode 上限"这笔账。
- 内容：LiteRT-LM 定位、软件栈分工与本书对 LiteRT 的讨论边界（自前言移入，v4.9）；
  隐私、离线和部署条件；内存容量、内存带宽、功耗与异构约束的量化分析；
  端侧运行时对照（llama.cpp / MLC / ExecuTorch / MediaPipe 与 LiteRT-LM 的不同选择）。
- 图表：图 1-1 LiteRT-LM 与 LiteRT 的主调用链（自前言移入）；图 1-2 三类物理约束与模型需求；
  表 1-1 软件栈分工（自前言移入）；表 1-2 端侧运行时对比（定位/后端/格式/使用场景）。
- 实证状态：已完成纸面计算，公式、单位与代入过程已写入正文；第 6 章用当前基准数据说明适用边界。
  LiteRT‑LM/LiteRT 调用关系已按 v0.13.1 的 `Environment`、`CompiledModel::Create` 与 `Run`/`RunAsync` 路径核验（自前言移入）。
- 缺口：收集 2-3 款典型 SoC 的带宽/算力参数（须注官方来源，按【文档】级）。

**第 2 章 从运行到架构：benchmark 指标解读与五层概览**（约 24 页）
- 使命：运行基准、正确解读性能数字，并建立全书的实现地图。
- 内容：CLI 跑 Gemma → `--benchmark` 逐项解读（Roofline 初讲：prefill=算力瓶颈、
  decode=带宽瓶颈）→ 「本书要回答的 20 个问题」清单（每问标注解答章节）→
  五层架构图 + 三条设计原则（接口隔离/可插拔后端/状态即对象）→
  `litertlm_print` 初步检查模型文件，为第 7 章的容器分析提供入口。
- 图表：图 2-1 Roofline 草图；图 2-2 五层架构总图；图 2-3 端到端数据流
  （学习网站 SVG 重制为书版，2026-07-17 作者拍板增补，本章图预算 2→3）；
  表 2-1 benchmark 输出字段解读；表 2-2 二十个问题 → 章节对照。
- 实证状态：已完成 Mac 主基准与 Android 扩展采集；方法、原始数据位置和无效字段均记录在附录 D。
- 素材：架构 SVG（已有）、synth.layers、engine.h 注释。

### 第二部分 · 推理流水线（3 章，约 70 页，全书主线）

**第 3 章 输入侧：从 Engine API 到 token 序列**（约 25 页）
- 使命：走通输入侧全程——API 设计、对话组装、模板渲染、分词。
- 核心问题：一个好的推理 API 长什么样？多轮对话如何不重复 prefill 历史（模板 diff 增量渲染）？
- 内容：Engine/Session 两级抽象与三层配置、EngineFactory 静态自注册；
  Conversation 的 Message(JSON)/Preface/历史管理；模板增量渲染 diff；
  SentencePiece 与 HuggingFace 两种分词；model_data_processor 工厂（gemma3/4、qwen3 格式差异）。
- 图表：图 3-1 输入侧数据流（Message → 模板 diff → token ids）；
  图 3-2 Engine/Session/Conversation 关系；表 3-1 各模型 data processor 格式差异。
- 实证状态：已完成 Engine、Conversation、模板 diff 与两类 tokenizer 实现的源码核验；
  尚未完成：同一输入的双 tokenizer 对照。原规划的“20 行最小 C++ 调用”和模板输出观察不作为当前基准结论。
- 素材：runtime/engine/、runtime/conversation/、components 的 tokenizer。

**第 4 章 Prefill：并行处理提示词**（约 22 页）
- 使命：理解 prefill 为什么快、静态/动态形状两条路径、以及支撑它的异步底座。
- 核心问题：固定长度 signature 有什么代价？v0.13.1 的 prefill 与 decode 分别在何时响应取消？
- 内容：Tasks::Prefill 流程；SortedPrefillSignatureMap 选择 / prefill_chunk_size 分块；
  异步执行底座（ExecutionManager/任务队列/任务依赖链）；
  任务开始前后的取消检查、decode 循环检查与 v0.13.1 尚未接线的 executor 参数。
- 图表：图 4-1 静态与动态 prefill 路径；图 4-2 prefill 与 decode 的取消时序；
  表 4-1 静态/动态两路径权衡。
- 实证状态：已完成 100→4000 token 的 prefill 长度扫描；取消检查点、异步提交条件与回调等待来自源码核验。
  原规划的 async 开/关性能对照未形成受控实验，正文不报告其性能收益。
- 素材：runtime/core/tasks.cc、session_advanced.cc、runtime/framework/execution_queue、
  executor 的 PrefillInternal。

**第 5 章 Decode：单步解码循环**（约 23 页）⭐ 样章
- 使命：走通 `DecodeOneStep` 的完整循环，解释一个 decode step 如何产生并输出 token。
- 内容：自回归循环与 ShouldStop；采样(greedy/top-k/top-p/温度)；logits 处理挂载点；
  停止序列前缀的暂存与失配释放；尚不能解码的 BPE token id 合并；
  非空增量的 `kProcessing` 回调与终态回调。
- 图表：图 5-1 DecodeOneStep 循环流程（内部/外部采样双路径）；
  图 5-2 单候选停止序列的暂存、失配释放与完整命中时序；表 5-1 采样策略对照。
- 实证状态：已完成温度与 seed 的输出观察；停止序列和未完整 BPE 序列依据源码及测试核验。
  原规划的“构造停止序列部分匹配用例”没有独立实验记录，不作为测量结果。
- 素材：tasks.cc（Tasks::Decode、DecodeOneStep）、top_p_cpu_sampler、stop_token_detector。

### 第三部分 · 运行时优化与异构执行（4 章，约 90 页）

**第 6 章 KV cache 与会话状态**（约 25 页）
- 使命：从内存账到双缓冲实现，再到会话上下文的克隆与回退。
- 核心问题：KV cache 有多大（算给读者看）？GPU 为什么要双缓冲？
  克隆一段对话为什么可以不重算？
- 内容：开篇 Roofline 深化（与第 1/2 章的账对上）；KV cache 结构与内存公式；
  由容量公式推导上下文增长时的 KV 逻辑扫描量；
  kv_cache_buffers_1_/2_ 指针互换免拷贝；`SharedProcessedContext` 与 handler 状态；
  `Session::Clone` 初始共享 `SharedProcessedContext`，后续分支 prefill 写入时才分离；
  分离路径中的 `CloneContext` 按 `PackedSize()` 复制完整已分配输入 KV 缓冲容量，而非只复制逻辑有效前缀；
  公共前缀分支的条件化容量案例；SaveCheckpoint / RewindToStep 的逻辑书签语义；
  thinking channel 内容从 KV 过滤（rewind 的应用）。
- 图表：图 6-1 KV cache 双缓冲指针互换；图 6-2 条件化 out-of-place 分支容量变化；
  图 6-3 Clone/Rewind 的状态分叉；表 6-1 KV 逻辑扫描量；表 6-2 预留宽度扫描；
  表 6-3 分支容量口径；表 6-4 状态复制与接口边界；表 6-5 checkpoint 行为。
- 实证状态：已完成 `--max-num-tokens` 扫描并记录同 prompt 下 4096/8192 的 decode 差异；
  Clone 的初始共享、写时分离和完整容量复制来自源码链路核验，不把 Clone 调用本身写成立即复制 KV。
- 素材：kv_cache_interface.h、executor 双缓冲成员、session_advanced 的 checkpoint。

**第 7 章 模型的形态：量化、.litertlm 格式与 LoRA**（约 30-34 页）
- 使命：理解模型的量化、容器封装与 LoRA 增量适配链路。
- 核心问题：int4 省的是什么（体积?带宽?算力?）？为什么要自造一个容器格式？
- 内容：int4/int8 量化收益账 + FP16 激活；从文件表示、TFLite 图、后端编译到 kernel 的支持链；
  2B 参数混合位宽部署容量案例；.litertlm 固定前缀、分段设计与两阶段写入
  （FlatBuffer header + sections + mmap 加载）；分段并行加载与冷启动；
  LoRA 离线合并与运行时适配（v0.13.1 保留已使用 adapter 的资源，未提供逐项卸载）；
  weight cache 的生成责任、命名与失效盲区；后端切换初始化失败诊断案例；
  Gemma 4 E4B 的 3.66 GB / 10 段容器解剖；从固定前缀到后端编译的分层校验；
  不可变模型名、独立 cache 目录与双进程回滚；N/P/W/S 冷热启动矩阵；
  35 层 rank-32 测试资产的 LoRA payload 复算与多适配器累计容量；
  双版本发布的存储/Engine 工作集峰值与单 Engine 顺序重启方案；
  制品、容器、cache、性能、内存、LoRA 与回滚的发布验收证据包。
- 图表：图 7-1 低比特表示到 kernel 的支持链；图 7-2 .litertlm 文件分段结构；
  图 7-3 mmap 与分段并行加载示意；图 7-4 weight cache 派生与失效边界；
  图 7-5 容器分层校验；图 7-6 模型与 cache 分代发布；图 7-7 LoRA ID 生命周期；
  图 7-8 分代发布资源峰值；
  表 7-1 量化精度收益账；表 7-2 量化部署逐层取证；表 7-3 cache 失效矩阵；
  表 7-4 LoRA signature 兼容性；表 7-5 初始化故障分层诊断；表 7-6 真实容器段；
  表 7-7 容器校验层；表 7-8 cache 入口；表 7-9 发布关口；表 7-10 冷热启动矩阵；
  表 7-11 LoRA buffer payload；表 7-12 多适配器资源累计；表 7-13 发布方式资源权衡；
  表 7-14 发布验收证据包；表 7-15 发布证据状态语义。
- 实证状态：已完成当前 `.litertlm` 文件的 section、signature 与张量形状分析，并记录 Init API 聚合值的批内变化；
  尚未完成：编译缓存开/关的外部墙钟受控对照、同模型 int4/int8 对照、`parallel_file_section_loading` 开/关对照。
  现有数据不能量化缓存、量化位宽或分段并行加载各自的收益。
- 素材：schema/core/、ActivationDataType、lora.cc/lora_manager.cc。

**第 8 章 异构算力：CPU、GPU 与 NPU**（约 24 页）
- 使命：说明三类后端的实现差异、运行时分派、buffer 交接条件与可复现诊断方法。
- 核心问题：同一模型换后端为什么行为/速度不同（`LiteRT-LM#2281`）？
  GPU 设备侧采样减少的数据传输量有多少？怎样区分 kernel、复制与同步成本？
- 内容：Backend 枚举与工厂分派；CPU(XNNPack) + 线程池/CPU 亲和性；
  GPU(OpenCL/Metal/WebGPU) 与设备侧采样；zero-copy 条件、显式复制与等待路径；
  端到端计时和受控 A/B；首个输出分叉 step 的 logits 诊断；NPU 专用执行器与 embedder 缓冲共享
  （结构行为以代码分析为主；两台 Qualcomm 设备已做部署探测，但均未完成 prefill 或 decode）。
- 图表：图 8-1 后端工厂分派；图 8-2 设备侧采样与 host 采样的数据路径对比；
  图 8-3 配置到编译选项与 buffer；图 8-4 buffer 直接交接与退化路径；
  表 8-1 buffer 交接条件与退化路径；表 8-2 CPU/GPU/NPU 特性权衡。
- 实证状态：已完成 CPU/GPU 同机对比和 1/2/4/8 线程扫描；两台设备分别探测到 QNN context 创建与 backend/device 创建阶段。
  尚未完成：设备侧采样与同步/复制的分项 microbenchmark、CPU/GPU 输出分叉的逐层复现，以及使用匹配模型产物、SoC 代际和运行时的 NPU prefill/decode；本书没有 NPU 吞吐、时延或功耗数据。
- 素材：executor factory、NPU executor、threadpool.cc、cpu_affinity_utils。

**第 9 章 一次前向，多个 token：推测解码与 MTP**（约 19 页）⭐ 重点章
- 使命：说明 drafter/verifier 机制，以及聚合接受比例与每轮运行成本如何共同决定端到端吞吐；
  官方博客报告的最高约 3 倍结果只作为注明条件的外部对照，不构成本书设备上的性能承诺。
- 核心问题：为什么推测解码可能提高吞吐？聚合接受比例与轮次成本在什么条件下使收益为负（解释 `LiteRT-LM#2227` 的 GPU 回退）？
- 内容：推测解码原理谱系（多模型与 MTP 单模型变体）；RunDraftingLoop/RunVerification；
  `num_drafted`/`num_verified` 的生命周期聚合接受比例；如何根据 .litertlm 的模型分段类型判断是否支持推测解码；
  多 token 返回后的逐 token 停止检测、批中回退、流式回调和数值上限边界。
- 图表：图 9-1 drafter/verifier 时序；图 9-2 条件匹配概率与理论收益曲线（示意参数，不是实测拟合）；
  图 9-3 多 token 批次的停止检测与逻辑位置回退；
  表 9-1 Mac 归档中的关闭行为对照；表 9-2 停止条件与执行时点；表 9-3 多 token 返回回归用例。
- 实证状态：已完成主基准开关记录、Android 合成负载复测，以及两个自然文本样本的聚合接受比例采样；
  自然代码吞吐与部分强制开启结果只有单次观测，不能估计稳定吞吐或稳定加速比，也不能用聚合接受比例单独归因。
  `LiteRT-LM#2227` 的 PowerVR 数据仍按【文档】级引用；本书 Qualcomm 真机只用于同类负收益对照。
- 素材：llm_litert_mtp_drafter、schema/capabilities/speculative_decoding。

### 第四部分 · 能力扩展与工程集成（2 章，约 48 页）

**第 10 章 多模态输入、约束解码与工具调用**（约 28 页）
- 使命：说明图像与音频如何从消息 API 变为主干 embedding，以及结构约束与宿主执行权限的边界。
- 内容：`Message` / `InputData` 所有权与生命周期；视觉/音频编码器 → embedding 注入序列
  （special token 对齐约定、patchify、shape 与分块）；多模态失败分层诊断；
  约束解码（llguidance，逐步屏蔽非法 token、状态推进与 tools-derived 文法覆盖）；
  Tool Use 全链路（`Preface.tools` 声明 → 模型输出 → parser → 宿主校验与授权 → 执行 → 结果回填）。
- 图表：图 10-1 图像 embedding 注入序列；图 10-2 约束解码逐步屏蔽；
  图 10-3 多模态数据边界与诊断检查点；图 10-4 工具调用信任边界；
  表 10-1 输入表示与所有权；表 10-2 多模态 shape 账本；
  表 10-3 tools-derived 约束边界；表 10-4 Tool Use 环节职责；表 10-5 宿主信任检查。
- 实证状态：已完成约束解码开/关的小样本观察，每种设置各 6 次，共 12 次；所测样本均结构合法，但该样本量不能估计成功率或失败率。
  尚未完成：图片输入、visual token 计数与 patchify 公式的多模态端到端验证。
- 素材：vision/audio executor、preprocessor、logits_processor/constrained_decoding、tool_use/。
- 产物：本书所用 Gemma 4 E4B LiteRT-LM 模型卡与模型产物公开可得；附录 D 以 SHA-256 固定实际分析文件。
- 缺口：多模态端到端尚无运行记录，现有结论来自源码、模型 section 与张量形状分析。
- 注：本章包含感知输入与受控输出两个主题，小节边界需明确。

**第 11 章 多语言绑定：C ABI、JNI 与 Embind**（约 20 页）
- 使命：比较 C ABI、JNI 与 Embind 三种原生边界，以及各自的所有权和并发约束。
- 内容：C ABI（不透明句柄/所有权/流式回调跨 FFI）；Python ctypes、Kotlin JNI、
  Swift actor、Web WASM 各自的封装；分别用 `LiteRT-LM#2589` 与 `#2613` 讨论 Conversation 和 Engine 生命周期；
  FakeLlmExecutor 可测试性设计；Bazel/CMake 双构建系统。
- 图表：图 11-1 三类原生边界与语言绑定结构；表 11-1 各语言 FFI 机制对照。
- 实证状态：已完成 C ABI、JNI、Embind 与现有 `FakeLlmExecutor` 测试路径的源码核验；
  尚未完成：同一 prompt 的 Python/C++ 行为一致性对照。原规划的新增 fake 用例不作为当前已完成实验。
- 素材：c/engine.h、python/_ffi.py、kotlin JNI、swift actor、fake_llm_executor。

### 尾声 · 实践入口与待验证问题（约 7 页）
- 四个集成路径（Python、Android/JVM、iOS/macOS、Web）；如何定位 issue；
  最小可比基线、分层故障定位与版本升级双基线；
  后续验证方向（低比特量化、NPU 部署、工具调用与多步编排）。
- 图表：图 尾-1 从模型产物到语言边界的部署诊断路径；
  表 尾-1 最小基线记录；表 尾-2 分层诊断对照；表 尾-3 版本升级双基线；
  表 尾-4 低比特、NPU 与工具调用的验证矩阵。

### 附录（当前成书约 25 页）
- 附录通则：正文已经引用的外部来源不在任何附录重复列出，附录之间也不重复；正文仍可正常指向附录中的术语、代码地图、复现步骤、基准数据和答案
- 附录 A 术语表（工作文件 `appendix/glossary.md` 随章累积，成书时定稿；学习网站 90 条为底本）
- 附录 B 代码地图（模块 → 文件 → 章节对照；学习网站内容的降级归宿）
- 附录 C 环境搭建与实验复现（`experiments/` 脚本的汇总说明）
- 附录 D 基准数据集（设备、模型、采集方法、原始数据与开放项）
- 附录 E 练习提示与参考答案

### 页数预算汇总

| 部分 | 章 | 页数 |
|---|---|---|
| 一 · 约束、指标与系统概览 | 1-2 | ~40 |
| 二 · 推理流水线 | 3-5 | ~70 |
| 三 · 运行时优化与异构执行 | 6-9 | ~90 |
| 四 · 能力扩展与工程集成 | 10-11 | ~48 |
| 尾声 | — | ~7 |
| 附录 | A-E | ~25 |
| **合计** | **11 章 + 尾声** | **约 285（区间 250-300）✓** |

### v2→v3 内容归并对照

| 原章（19 章版） | 去向 |
|---|---|
| 原 8 Roofline | → 第 2 章初讲 + 第 6 章开篇深化 |
| 原 13 线程/内存/调度 | → 异步底座入第 4 章；线程池/亲和性入第 8 章；并行加载入第 7 章 |
| 原 16 状态管理 | → Clone/Checkpoint/channel 入第 6 章；LoRA 入第 7 章；历史入第 3 章 |
| 原 17+18 绑定/工程 | → 合并为第 11 章 |
| 原 19 展望 | → 缩为尾声 |
| 原 4+5 / 原 14+15 | → 各自合并为第 3 章 / 第 10 章 |

**压缩后未单独展开的内容**：构建系统细节（Bazel/CMake 只保留一节）；
NPU 独立展开（并入第 8 章一节）；框架层线程实现细节（保留设计思想，略去代码级展开）。
若日后出"完整版/v2"，这些是首选扩展点。

### 「20 个问题」清单（第 2 章的牵引装置，兼作目录覆盖度自检）

> 状态：已写入第 2 章。每问必须在标注章节得到正面回答。

| # | 读者的问题 | 解答章节 |
|---|---|---|
| 1 | 同样的模型，为什么云端可以流畅运行，手机端通常更受限制？ | 第 1 章 |
| 2 | 4B 参数的模型，8 GiB 内存的手机放得下吗？ | 第 1、7 章 |
| 3 | 为什么 prefill 每秒几千 token，decode 只有几十？ | 第 2 章初讲，第 6 章深化 |
| 4 | time-to-first-token 由哪几段时间构成？ | 第 2 章 |
| 5 | Engine 和 Session 为什么要分成两层？ | 第 3 章 |
| 6 | 多轮对话的历史，每一轮都要重新计算吗？ | 第 3 章（模板 diff）、第 6 章（KV cache） |
| 7 | 聊天模板是谁、在什么时候套上去的？ | 第 3 章 |
| 8 | 模型文件里为什么有好几个固定长度的 prefill 入口？ | 第 4 章 |
| 9 | 生成中途取消时，延迟取决于哪些检查点？ | 第 4 章 |
| 10 | 温度、top-k、top-p 各自改变了什么？ | 第 5 章 |
| 11 | 流式输出为什么偶尔会出现不完整的 UTF-8 字节序列？ | 第 5 章 |
| 12 | 停止序列只匹配了前缀时，已生成内容是否立即输出？ | 第 5 章 |
| 13 | KV cache 占多少内存？`--max-num-tokens` 为什么影响速度？ | 第 6 章（`LiteRT-LM#2568`） |
| 14 | 克隆对话做分叉，需要重算公共前缀吗？ | 第 6 章 |
| 15 | int4 量化省的是体积、带宽还是算力？ | 第 7 章 |
| 16 | .litertlm 单文件里都装了什么？ | 第 7 章 |
| 17 | 换个后端，速度甚至输出为什么都会变？ | 第 8 章（`LiteRT-LM#2281`） |
| 18 | GPU 设备侧采样比 host 采样减少了什么？ | 第 8 章 |
| 19 | 推测解码为什么可能提高吞吐？什么条件下收益为负？ | 第 9 章（`LiteRT-LM#2227`） |
| 20 | 模型怎么处理图片？约束解码能保证什么？ | 第 10 章 |

覆盖度自检结论：11 章中除第 11 章外均被清单覆盖；第 11 章的问题（“C++ 核心如何连接不同语言运行时？”）偏集成向，刻意不入面向推理读者的主清单，
在第 2 章正文以"工程读者另见第 11 章"一句带过。

---

## 四、历史路线图（六阶段，含验收门）

> 本节保留立项时的阶段划分、工期估计与验收门，用于追溯写作路线，不表示当前完成度。当前状态以文首、各章卡的“实证状态”和附录 D 为准。

| 阶段 | 内容 | 时长 | 验收门 |
|---|---|---|---|
| **P0 决策** | 拍板 D1/D2/D4；冻结目录 v3；书名候选 | 1 周 | 每章通过"一句话使命"测试 |
| **P1 内容与规范基建** | 冻结 tag（建议 v0.13.1）并回填规范示例；选工具链（mdBook 或 Quarto）建私有仓；**规范基建**：`assets/book.css`（从学习网站复制变量表）、`scripts/lint_prose.sh`（禁词/填充词机检）、`appendix/glossary.md` 初始化、`review.md` 模板；data.json 素材迁入各章 notes.md；补读清单（tasks.cc、llm_litert_compiled_model_executor.cc、conversation.cc、litertlm_read.cc、llm_litert_mtp_drafter.cc、vision/audio executor 实现）；按下方规格 v1 采集基准数据集 | 2-3 周 | ⭐ 第 5 章样章通过下方"样章验收判据"全部四条 |
| **P2 推理主线** | 第 3-5 章 | 3-4 周 | 内部版 v0.1（私有 tag）；自读复盘，可邀 1-2 位信任同行试读 |
| **P3 运行时优化与异构执行** | 第 6-9 章（实验最多） | 5-6 周 | 实验脚本全部入库可复现 |
| **P4 能力扩展与工程集成** | 第 10-11 章 + 尾声 | 3 周 | 内部版 v0.3 |
| **P5 整稿** | 第 1-2 章（最后写开头）；附录；统稿（含术语首现按成书顺序的专项校正 pass、图表编号与交叉引用核对） | 2-3 周 | 完整通读修订 → 内部版 v1.0 |
| **P6 开源/出版评估** | 核心版打磨满意后启动（非并行前置） | 待定 | 由作者决策是否开源、是否接触出版社 |

**原规划工期**：约 4-5.5 个月出完整初稿（每周 0.7-1 章）。

**原规划的核心版里程碑**：第一至三部分（9 章，约 200 页）构成可独立验收的核心范围；
第四部分、尾声与附录补充扩展能力和工程内容。是否兑现以对应章节的事实核查与附录 D 实验状态为准。

**原规划写作顺序**：样章(5) → 推理主线(3-4) → 运行时优化与异构执行(6-9) → 能力扩展与工程集成(10-11) → **最后写第 1、2 章**。
第 1、2 章安排在主体完成后撰写，以便开篇范围与全书实际内容一致。

**基准数据集规格 v1（原规划）**（P1 采集；脚本入 `experiments/bench_baseline.sh`，原始数据入 `experiments/data/`，说明入附录 D）：
- 主基准设备：作者的 Mac（Apple Silicon；具体型号/内存/系统版本随采集记录）
- 主基准模型：**Gemma 4 E4B**（`litert-community/gemma-4-E4B-it-litert-lm`，公开非受限、支持 MTP；主模型即覆盖第 9 章 MTP 实测，无需另找）
- 条件矩阵：backend ∈ {cpu, gpu} × 上下文 ∈ {256, 1024, 4096} × MTP ∈ {关, 开（仅支持的模型）}；每条件跑 3 次取中位数
- 指标：prefill tokens/s、decode tokens/s、TTFT (ms)、峰值内存（`--report_peak_memory_footprint`）、冷启动加载时间
- 纪律：全书正文只引用本数据集与注明出处的官方数据（`AGENTS.md` 第二节）；扩展基准（如 Android 真机）单独标注，不与主基准混算
- 可得性：litert-community 的 Gemma 4 版**非受限**，直接可下（约 3.4 GB），无需 HF 登录；google/ 官方版才受限

当前采集没有完全沿用原规划矩阵：CPU/GPU 主基准的三个上下文已各运行 3 次；Mac 的 `false`/`auto` 组有记录，但没有可核查的强制 MTP 组。自然文本和 Android 扩展使用各自注明的次数与入口。CLI 未提供可用的主基准峰值内存列，Android 峰值内存旧记录也因解析错误作废；冷启动目前只有 Init API 聚合值的批内变化，缺少缓存开/关与外部墙钟受控对照。实际口径见附录 D。

**样章（第 5 章）原规划验收判据**（四条全过才算通过 P1 门）：
1. `review.md` 中断言归级完成率 100%，代码引用逐条 Read 核验通过；
2. `lint_prose.sh` 零命中；除 AI 味清单逐项打钩，第 14 条由独立会话执行；
3. 章卡的使命、图表清单（2 图 1 表）、实验脚本全部兑现且可一键复现；
4. 作者通读认可口吻与深度——通过后该章即全书"深度标尺"，后续章节向它对齐。

---

## 五、风险登记册

| 风险 | 概率 | 对策 |
|---|---|---|
| 上游演进导致内容过时 | 高 | 锁 tag +「版本注记」侧栏；讲设计与权衡而非易变细节 |
| 素材深度不足 | 中 | P1 补读清单硬性完成；章卡"缺口"栏跟踪 |
| 单人项目未按计划完成 | 中高 | 核心版里程碑 + 每章完成即打私有 tag + 固定双周自查复盘 + 验收看 review.md 留痕 + 样章先行 |
| 厚章写作失焦 | 中 | 每章先立 4-6 个小节的节级大纲再动笔；单节 ≤6 页 |
| 规范执行漂移（多会话乱序写作） | 中 | `AGENTS.md` 逐会话加载；`lint_prose.sh` 机检；除 AI 味第 14 条由独立会话执行 |
| 与官方文档重叠 | 低 | 坚守"实现与权衡"视角 |
| NPU 部署探测被误写成推理验证 | 中 | 明确两台设备只到达初始化阶段；prefill/decode、吞吐、时延与功耗仍标为未完成，结构行为以代码分析为主 |
| 单人精力波动 | 高 | notes.md 与正文分离，读码与写作分时段；内部评审以"节"为粒度 |

---

## 六、历史行动清单与当前未结项

> P0/P1 清单按当时记录保留；其中候选书名等中间状态由后续勾选项结清。当前实验未结项统一列在文首与附录 D，不以本节旧措辞判定完成度。

**P0 收尾**：
- [x] 拍板 D1（体例）、D2（读者）、D4（语言）——2026-07-05 作者确认
- [x] 逐章过"一句话使命"测试：11 章全部通过（第 2/10 章为刻意的复合使命章，
      约束已写入章卡），目录 v3 冻结——2026-07-05
- [x] 书名候选 3 个（含英文名），待作者选定：
      - **候选 A（描述型，推荐）**：《端侧大模型推理：原理与 LiteRT-LM 实现》
        *On-Device LLM Inference: Principles and Practice with LiteRT-LM*
        —— 检索友好、定位精准、出版社友好；缺点是平实。
      - **候选 B（叙事型）**：《深入端侧大模型推理》
        *Inside On-Device LLM Inference*
        —— 标题较短；缺点是书架上看不出主题词 "LiteRT-LM"。
      - **候选 C（问题型）**：《大模型如何跑进手机：LiteRT-LM 深度解析》
        *How LLMs Run on Your Phone: A Deep Dive into LiteRT-LM*
        —— 用读者的问题当书名；"跑进手机"具体不夸大，但口语化程度最高。
      - 推荐组合：A 为主书名；B 的意象留给前言与第二部分标题。
- [x] 作者选定书名：**候选 A**《端侧大模型推理：原理与 LiteRT-LM 实现》——2026-07-05。
      P0 全部完成 ✅

**P1 开工项**（依赖 P0 完成）：
- [x] 冻结 tag = v0.13.1，回填 `AGENTS.md` 示例（两处示例行号已按 v0.13.1 核验：h:329、engine.h:238）——2026-07-05
- [x] 选定工具链：**mdBook**（内部打磨轻快优先；正文纯 markdown，日后可平迁 Quarto 出 PDF）；仓库 `git init` 本地私有——2026-07-05
- [x] 建规范基建：`assets/book.css`、`scripts/lint_prose.sh`、`appendix/glossary.md`(种子)、`chapters/_shared/review-template.md`——2026-07-05
- [x] 章节骨架 + data.json 素材迁移：`chapters/_shared/module-*.md`(12) + `synth-architecture.md` 为单一事实源，各章 `notes.md` 做策展层，`chapter.md` 骨架就位（`scripts/gen_scaffold.py` 可幂等重生成）——2026-07-05
- [x] 补读 6 个文件组（tasks.cc、llm_litert_compiled_model_executor.cc、conversation.cc、litertlm_read.cc、llm_litert_mtp_drafter.cc、vision/audio executor 实现）；v4.8 已在对应章节落下代码锚点
- [x] 基准数据集采集（Mac 主矩阵与 Android 扩展均已入附录 D；开放项另列）
- [x] 第 5 章样章（后续事实修订纳入 v4.8）
